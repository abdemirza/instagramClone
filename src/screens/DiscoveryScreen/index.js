import React from 'react';
import {View, Text} from 'react-native';

const DiscoveryScreen = () => {
  return (
    <View style={{backgroundColor: 'blue', height: 1500,alignContent:'center'}}>
      <Text style={{fontSize:30,color:'white',marginTop:200,textAlign:'center'}}>Discovery</Text>
    </View>
  );
};

export default DiscoveryScreen;
